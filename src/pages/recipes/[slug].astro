---
import JsonLdSchema from "../../components/JsonLdSchema.astro";
import Icon from "../../components/Icon.astro";
import IngredientList from "../../components/IngredientList.astro";
import RecipeSteps from "../../components/RecipeSteps.astro";
import CookwareList from "../../components/CookwareList.astro";
import CooklangSourceBlock from "../../components/CooklangSourceBlock.astro";
import Layout from "../../layouts/Layout.astro";
import { buildRecipeJsonLd } from "../../utils/recipe-metadata";
import { getAllRecipes, slugifyTag, extractSteps } from "../../lib/recipes";

export async function getStaticPaths() {
  // Single shared loading pipeline (recursive discovery, frontmatter merge).
  return getAllRecipes().map((recipe) => ({
    params: { slug: recipe.slug },
    props: { recipe },
  }));
}

const { recipe: loaded } = Astro.props;
const { slug } = loaded;
// `recipe` keeps its historical shape (parsed Recipe with merged metadata)
// so the template below stays unchanged.
const recipe = loaded.parsed;

const ingredients = recipe.ingredients || [];
const cookwareList = recipe.cookware || [];
const timersList = recipe.timers || [];

// Extract steps from all sections, in order
const steps = extractSteps(recipe);

const imageUrl = recipe.metadata.image?.replace(/^["']|["']$/g, "") || null;
const tags: string[] = recipe.metadata.tags || [];
const source = recipe.metadata.source;

// Generate JSON-LD schema (cookware/timers forwarded correctly now)
const jsonLd = buildRecipeJsonLd({ slug, title: loaded.title, parsed: recipe });
---

<Layout title={recipe.metadata.title || slug} description={recipe.metadata.description} image={imageUrl}>

  <Fragment slot="head">
    <JsonLdSchema schema={jsonLd} />
  </Fragment>

  <div class="recipe-header" data-pagefind-body>
    <h1>
      {recipe.metadata.title || slug}
      <button
        type="button"
        class="fav-star"
        data-fav
        data-fav-slug={slug}
        aria-pressed="false"
        aria-label={`Add ${recipe.metadata.title || slug} to favorites`}
        title="Favorite"
      >★</button>
    </h1>
    {recipe.metadata.description && <p class="recipe-description">{recipe.metadata.description}</p>}

    {tags.length > 0 && (
      <div class="tags">
        {tags.map((tag: string) => (
          <a href={`${import.meta.env.BASE_URL}tags/${slugifyTag(tag)}/`} class="tag">
            #{tag}
          </a>
        ))}
      </div>
    )}

    {source && (
      <div class="source">
        <div class="source-label">Source:</div>
        <a href={source} target="_blank" rel="noopener noreferrer">{source}</a>
      </div>
    )}

    <div class="recipe-controls" data-recipe-controls data-pagefind-ignore>
      <a class="button primary" href={`${import.meta.env.BASE_URL}recipes/${slug}/cook/`}>
        <Icon name="list" size={18} /> Start cooking
      </a>
      <button type="button" class="button" id="addToShoppingListBtn"
              data-recipe-slug={slug}
              data-recipe-title={recipe.metadata.title || slug}>
        <Icon name="cart" size={18} /> Add to Shopping List
      </button>
      <button type="button" class="button icon-only" id="recipePrintBtn" aria-label="Print recipe" title="Print">
        <Icon name="printer" size={18} />
      </button>

      <div class="scale" role="group" aria-label="Recipe scaling">
        <button type="button" class="scale-btn" id="scaleMinus" aria-label="Decrease scale">−</button>
        <input
          id="recipeScaleInput"
          type="number"
          inputmode="decimal"
          step="0.1"
          min="0.1"
          max="99"
          value="1"
          aria-label="Scale factor"
        />
        <span class="scale-suffix" aria-hidden="true">×</span>
        <button type="button" class="scale-btn" id="scalePlus" aria-label="Increase scale">+</button>
      </div>
      <span class="scaled-servings" id="scaledServings" aria-live="polite"></span>
      <p class="print-scale-note" id="printScaleNote" aria-hidden="true"></p>
    </div>
  </div>

  <div class="hero-section" data-pagefind-body>
    <div class="recipe-image-container">
      {imageUrl ? (
        <img
          src={imageUrl}
          alt={recipe.metadata.title || slug}
          class="recipe-image"
          onerror="this.style.display='none';this.nextElementSibling.style.display='flex';"
          loading="lazy"
        />
      ) : null}
      <div class="image-fallback" style={imageUrl ? "display: none;" : "display: flex;"}>
        <Icon name="recipes" size={44} class="fallback-icon" />
        <span class="fallback-text">No Image</span>
      </div>
    </div>

    <div class="recipe-meta">
      {recipe.metadata?.["total-time"] && (
        <span class="meta-chip">
          <Icon name="clock" size={15} />
          {recipe.metadata["total-time"]}
        </span>
      )}
      {recipe.metadata?.["prep-time"] && (
        <span class="meta-chip">
          prep {recipe.metadata["prep-time"]}
        </span>
      )}
      {recipe.metadata?.["cook-time"] && (
        <span class="meta-chip">
          cook {recipe.metadata["cook-time"]}
        </span>
      )}
      {recipe.metadata?.servings && (
        <span class="meta-chip">
          <Icon name="recipes" size={15} />
          <span data-base-servings>{recipe.metadata.servings}</span> servings
        </span>
      )}
    </div>
  </div>

  <div class="recipe-content" data-pagefind-body>
    <div class="recipe-column">
      <div class="recipe-section">
        <h2 class="section-title"><Icon name="cart" size={20} /> Ingredients</h2>
        <IngredientList ingredients={ingredients} />
      </div>

      {cookwareList.length > 0 && (
        <div class="recipe-section">
          <h2 class="section-title"><Icon name="list" size={20} /> Cookware</h2>
          <CookwareList cookware={cookwareList} />
        </div>
      )}
    </div>

    <div class="recipe-column">
      <div class="recipe-section">
        <h2 class="section-title"><Icon name="check" size={20} /> Instructions</h2>
        {steps.length > 0 ? (
          <RecipeSteps parsed={recipe} />
        ) : (
          <p>No instructions found</p>
        )}
      </div>
    </div>
  </div>

  <CooklangSourceBlock slug={slug} text={loaded.rawContent} />

  <Fragment slot="scripts">
    <script type="module">
      import { formatQty } from '../../lib/quantities';
      import { applyScale } from '../../scripts/recipe-scaler.ts';
      import { recordRecent, isFavorite, toggleFavorite } from '../../scripts/library-store';

      recordRecent(document.querySelector('[data-fav]')?.getAttribute('data-fav-slug') ?? '');
      document.querySelectorAll<HTMLElement>('[data-fav]').forEach((btn) => {
        const slug = btn.dataset.favSlug ?? '';
        btn.setAttribute('aria-pressed', String(isFavorite(slug)));
        btn.addEventListener('click', () => {
          const active = toggleFavorite(slug);
          btn.setAttribute('aria-pressed', String(active));
        });
      });

      const controls = document.querySelector("[data-recipe-controls]");
      if (controls) {
        const printBtn = document.getElementById("recipePrintBtn");
        const scaleInput = document.getElementById("recipeScaleInput");
        const scaledServings = document.getElementById("scaledServings");
        const printScaleNote = document.getElementById("printScaleNote");

        // Base servings from metadata (may be "4" or "4 servings").
        const metaServings = document.querySelector("[data-base-servings]");
        const baseServings = metaServings ? parseFloat(metaServings.textContent || "") : NaN;

        function currentScale() {
          const mult = Number(scaleInput?.value || "1");
          return Number.isFinite(mult) && mult > 0 ? mult : 1;
        }

        function updateScaleUI() {
          const mult = currentScale();
          applyScale(mult);
          if (scaledServings) {
            scaledServings.textContent =
              Number.isFinite(baseServings)
                ? `${formatQty(baseServings * mult)} servings`
                : `\u00d7${formatQty(mult)}`;
          }
          if (printScaleNote) {
            printScaleNote.textContent = mult === 1 ? "" : `Scaled \u00d7${formatQty(mult)}`;
          }
        }

        if (printBtn) printBtn.addEventListener("click", () => window.print());

        if (scaleInput) {
          scaleInput.addEventListener("input", updateScaleUI);

          // Stepper buttons: \u00b10.5 within [0.1, 99]
          const step = (delta) => {
            const next = Math.min(99, Math.max(0.1, Math.round((currentScale() + delta) * 10) / 10));
            scaleInput.value = String(next);
            updateScaleUI();
          };
          document.getElementById("scaleMinus")?.addEventListener("click", () => step(-0.5));
          document.getElementById("scalePlus")?.addEventListener("click", () => step(0.5));

          updateScaleUI();
        }

        // Shopping list functionality
        const addToShoppingListBtn = document.getElementById("addToShoppingListBtn");
        if (addToShoppingListBtn) {
          import('../../scripts/recipe-shopping-list.ts').then(module => {
            const recipeSlug = addToShoppingListBtn.getAttribute('data-recipe-slug');
            const recipeTitle = addToShoppingListBtn.getAttribute('data-recipe-title');

            module.initializeShoppingListButton(addToShoppingListBtn, recipeSlug);
            module.setupShoppingListButton(addToShoppingListBtn, recipeSlug, recipeTitle);
          });
        }
      }
    </script>
  </Fragment>
</Layout>

<style>
  .recipe-header {
    margin-bottom: 1rem;
  }

  .recipe-header h1 {
    font-size: 2rem;
    font-weight: 800;
    margin-bottom: 0.35rem;
  }

  .recipe-description {
    color: var(--text-2);
    font-size: 1.05rem;
    margin: 0.35rem 0 0;
    max-width: 60ch;
  }

  .section-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.15rem;
  }

  .section-title :global(svg) {
    color: var(--primary);
  }

  .button.icon-only {
    padding: 0.5rem 0.6rem;
  }

  .button :global(svg) {
    flex-shrink: 0;
  }

  .fav-star {
    border: 0;
    background: transparent;
    color: var(--border);
    font-size: 1.2rem;
    line-height: 1;
    cursor: pointer;
    vertical-align: middle;
    padding: 0.1em 0.2em;
    transition: color 0.15s ease, transform 0.15s ease;
  }

  .fav-star[aria-pressed="true"] {
    color: #ffd166;
  }

  .fav-star:hover {
    transform: scale(1.15);
  }

  /* Recipe controls */
  .recipe-controls {
    display: flex;
    gap: 1rem;
    align-items: center;
    flex-wrap: wrap;
    margin: 1rem 0 0.5rem;
  }

  .recipe-controls .scale {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--surface);
    border: 2px solid var(--border);
    padding: 0.4rem 0.6rem;
    border-radius: 8px;
  }

  .recipe-controls .scale-label {
    color: var(--muted);
    font-size: 0.9rem;
  }

  .recipe-controls #recipeScaleInput {
    width: 5.5rem;
    border: 0;
    outline: none;
    font-size: 1rem;
    background: transparent;
    color: var(--input-text);
  }

  .recipe-controls .scale-suffix {
    color: var(--muted);
    font-weight: 700;
  }

  .recipe-controls .scale-btn {
    border: 0;
    background: var(--surface);
    color: var(--text);
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 6px;
    font-size: 1.1rem;
    font-weight: 700;
    line-height: 1;
    cursor: pointer;
  }

  .recipe-controls .scale-btn:hover {
    background: var(--border);
  }

  .scaled-servings {
    color: var(--muted);
    font-size: 0.95rem;
  }

  .cook-link {
    background: var(--accent);
  }

  .cook-link:hover {
    background: var(--primary-darken);
  }

  .print-scale-note {
    display: none;
  }

  /* Image + meta */
  .hero-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin: 1.5rem 0;
  }

  @media (max-width: 768px) {
    .hero-section {
      grid-template-columns: 1fr;
    }
  }

  .recipe-image-container {
    display: flex;
    align-items: flex-start;
  }

  .recipe-image {
    max-width: 100%;
    width: 100%;
    height: auto;
    border-radius: 8px;
    object-fit: cover;
  }

  /* Image fallback styling */
  .image-fallback {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    min-height: 300px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    gap: 0.5rem;
  }

  .fallback-icon {
    color: var(--text-2);
    opacity: 0.6;
  }

  .fallback-text {
    font-size: 1rem;
    color: var(--muted);
    font-family: system-ui, sans-serif;
  }

  .recipe-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    align-content: start;
  }

  .meta-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    background: var(--surface-2);
    border-radius: 999px;
    padding: 0.25rem 0.75rem;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-2);
  }

  .meta-chip :global(svg) {
    color: var(--primary);
  }

  .recipe-content {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.6fr);
    gap: 0;
    margin: 2rem 0 0;
    border-top: 1px solid var(--border);
  }

  .recipe-column {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 1.5rem 1.5rem 1.5rem 0;
  }

  /* Mealie's border-e-thin pattern: one quiet divider, no card boxes */
  .recipe-column:first-child {
    position: sticky;
    top: calc(var(--nav-h) + 1rem);
    align-self: start;
    max-height: calc(100vh - var(--nav-h) - 2rem);
    overflow-y: auto;
    border-right: 1px solid var(--border);
    padding-right: 1.5rem;
  }

  .recipe-column:last-child {
    padding-left: 1.5rem;
  }

  @media (max-width: 968px) {
    .recipe-content {
      grid-template-columns: 1fr;
    }

    .recipe-column:first-child {
      position: static;
      max-height: none;
      border-right: 0;
      border-bottom: 1px solid var(--border);
      padding-right: 0;
    }

    .recipe-column:last-child {
      padding-left: 0;
    }
  }

  .recipe-section h2 {
    color: var(--text-1);
    padding-bottom: 0.5rem;
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 1rem 0;
  }

  .tag {
    background: var(--surface);
    color: var(--accent-2);
    padding: 0.25rem 0.75rem;
    border-radius: 16px;
    font-size: 0.85rem;
    text-decoration: none;
    border: 1px solid var(--accent-2);
    transition: all 0.2s ease;
  }

  .tag:hover {
    background: var(--accent-2);
    color: white;
    transform: translateY(-1px);
  }

  /* Dark mode tag adjustments */
  html[data-theme="dark"] .tag {
    color: var(--teal);
    border-color: var(--teal);
  }

  html[data-theme="dark"] .tag:hover {
    background: var(--primary);
    color: var(--surface);
  }

  .source {
    background: var(--surface);
    border-left: 3px solid var(--accent);
    padding: 1rem;
    margin: 1rem 0;
    border-radius: 4px;
  }

  .source-label {
    font-size: 0.85rem;
    color: var(--accent);
    font-weight: bold;
    margin-bottom: 0.25rem;
  }

  .source a {
    color: var(--accent-2);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .source a:hover {
    text-decoration: underline;
    color: var(--accent-2);
    filter: brightness(1.2);
  }

  /* Dark mode source link adjustments */
  html[data-theme="dark"] .source a {
    color: var(--teal);
  }

  html[data-theme="dark"] .source a:hover {
    color: var(--teal);
  }

  /* Back button styling */




  /* Recipe image responsive styling */
  .recipe-image-container {
    position: relative;
    overflow: hidden;
    border-radius: 8px;
  }

  @media print {
    .recipe-header .back-button,
    .recipe-controls .button,
    .recipe-controls .scale,
    .scaled-servings,
    .theme-toggle-container,
    .cooklang-source,
    .source {
      display: none !important;
    }

    /* Keep the scale visible in print via the note (e.g. "Scaled \u00d72") */
    .print-scale-note {
      display: block;
      font-size: 1rem;
      font-weight: 700;
      margin: 0 0 0.5rem;
    }

    body {
      background: white;
      color: black;
      padding: 0;
      max-width: none;
    }

    .recipe-content {
      grid-template-columns: 1fr;
    }

    .recipe-section {
      break-inside: avoid;
    }

    .step {
      break-inside: avoid;
    }
  }

  /* Notification animations */
  @keyframes slideIn {
    from {
      transform: translateX(100%);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes slideOut {
    from {
      transform: translateX(0);
      opacity: 1;
    }
    to {
      transform: translateX(100%);
      opacity: 0;
    }
  }
</style>
